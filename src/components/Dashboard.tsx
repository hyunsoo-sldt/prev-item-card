import React from 'react';
import ContentWrapper from './ContentWrapper';

type DashboardProps = {};

const Dashboard = (props: DashboardProps) => {
    return <ContentWrapper>대시보드</ContentWrapper>;
};

export default Dashboard;
