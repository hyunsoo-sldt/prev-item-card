import React from 'react';
import ContentWrapper from './ContentWrapper';

type AlexProps = {};

const Alex = (props: AlexProps) => {
    return <ContentWrapper children={'안녕 난 알렉스야. 나 알지? 우결 했었자나ㅋㅋ'} />;
};

export default Alex;
