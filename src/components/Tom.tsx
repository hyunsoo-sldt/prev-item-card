import React from 'react';
import ContentWrapper from './ContentWrapper';

type TomProps = {};

const Tom = (props: TomProps) => {
    return <ContentWrapper>나 톰이야. 내가 짱이야.</ContentWrapper>;
};

export default Tom;
