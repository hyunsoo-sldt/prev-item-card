import React from 'react';
import ContentWrapper from './ContentWrapper';

type BillProps = {};

const Bill = (props: BillProps) => {
    return <ContentWrapper>저요?? 저 빌인데요??</ContentWrapper>;
};

export default Bill;
