import React from 'react';
import ContentWrapper from './ContentWrapper';

type DashboardProps = {};

const Global = (props: DashboardProps) => {
    return <ContentWrapper>글로벌</ContentWrapper>;
};

export default Global;
